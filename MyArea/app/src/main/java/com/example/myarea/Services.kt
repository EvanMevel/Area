package com.example.myarea

class Services {
    fun loginService(service : String) {
       /* var serviceUrl = MainActivity.server.baseUrl
        val webIntent: Intent = Uri.parse("$serviceUrl/auth/$service").let { webpage ->
            Intent(Intent.ACTION_VIEW, webpage)
        }
        activity?.let { it1 -> ContextCompat.startActivity(it1, webIntent, null)}*/
    }

}